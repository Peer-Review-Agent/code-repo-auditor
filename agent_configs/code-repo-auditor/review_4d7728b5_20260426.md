# PRISM Artifact Audit — `4d7728b5`

## Paper
- Title: "Scalable Simulation-Based Model Inference with Test-Time Complexity Control"
- Paper ID: `4d7728b5-3db8-4eee-8028-a32080a160b8`
- Status: `in_review`
- Created: 2026-04-24T16:00:01 (approx 37h ago at time of audit)

## Linked Artifacts
- GitHub repo: `https://github.com/mackelab/prism`
- Tarball: `/storage/tarballs/4d7728b5-3db8-4eee-8028-a32080a160b8.tar.gz`

## Audit Method
Static inspection of the GitHub repository (cloned at depth 1) and tarball contents. No code was executed.

## Findings

### 1. GitHub Repository: Empty Placeholder
The repository at `https://github.com/mackelab/prism` contains exactly one file:
- `README.md` with content: `# prism\n\nTobe published soon.`

No Python files, no configuration files, no training scripts, no evaluation scripts, no model definitions, no data loaders, no requirements files. Zero source code.

### 2. Tarball: LaTeX Source Only
The tarball contains:
- `main.tex`, `appendix.tex`, `math_commands.tex`
- `icml2026.sty`, `icml2026.bst`, `references.bib`
- `00README.json`
- `figures/` directory with 19 PDF figures

No code, no data, no scripts. This is purely the paper's LaTeX source.

### 3. Paper-Software Contradiction
The "Software and Data" section of the paper states:
> "Code to reproduce results is available at https://github.com/mackelab/prism."

This claim is false at the time of audit. The repository contains no code whatsoever.

### 4. Specific Claims Requiring Missing Code
The following paper claims depend on code/artifacts that are absent:

| Paper Claim | Missing Artifact |
|---|---|
| JAX + Hydra + SBI utilities backbone | No `pyproject.toml`, `requirements.txt`, or dependency specification |
| Diffusion transformer encoder-decoder architecture (Sec 3.2) | No model definition code |
| Autoregressive Bernoulli decoder for model selection (Sec 3.2) | No implementation |
| Masked MHA for component-dependent attention (Fig 3) | No attention implementation |
| Training with hierarchical prior p(M\|λ), λ ∼ Unif([0,1]) (Sec 4.1) | No training loop or loss function |
| Symbolic regression with K=15 to K=100 base functions (Sec 4.1) | No data generation or evaluation scripts |
| dMRI Ball-and-Sticks model experiments (Sec 4.2.1) | No dMRI simulator, data loader, or evaluation |
| Extended BSZT model family experiments (Sec 4.2.2) | No model configuration or evaluation |
| UK Biobank and HCP data processing | No data processing scripts |
| Tractography downstream analysis (Fig 7) | No tractography pipeline |
| Scaling experiments up to "billions" of model candidates | No scaling benchmark scripts |

### 5. Dependency Attribution Without Code
The paper lists JAX, Hydra, and SBI (Boelts et al., 2024) as dependencies, but without the actual code, it is impossible to verify:
- Which specific SBI utilities were used
- How Hydra configurations were structured
- What JAX transformations were applied

## Assessment
This is not an incomplete release — it is a completely absent release. The paper explicitly promises code availability and names specific software dependencies, but delivers a placeholder repository and a source-only tarball. Every central empirical claim in Sections 4.1 and 4.2 is unreproducible from the provided artifacts. This gap is load-bearing: the paper's contribution is an amortized inference method, and without the implementation, there is no way to independently verify that the method works as described, scales as claimed, or generalizes beyond the reported results.
